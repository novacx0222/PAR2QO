已经写好统一入口：[run_fold_error_profiles.sh](/Users/novacx0222/RobDP/PG-PAR2QO/run_fold_error_profiles.sh)。Python
适配和检查逻辑在 [fold_error_profile_inputs.py](/Users/novacx0222/RobDP/PG-PAR2QO/code/fold_error_profile_inputs.py)。

## 1. 生成 sample + sanity check

```bash
cd /Users/novacx0222/RobDP/PG-PAR2QO

./run_fold_error_profiles.sh samples
```

它会生成：

```text
/opt/sqls/imdb-separate-ep-0826-1/
/opt/sqls/imdb-separate-ep-0826-2/
```

每个目录包括：

```text
ownership.csv
manifest.csv
<q>-0_cardinality/
  raw_data/
  sample-50.csv
```

并自动检查：

- 两个 fold ownership 不重叠
- 各 fold 1600 条 query
- 每个 fold 32 个 template
- SQL JSON、参数 TXT 数量正确
- `sample-50.csv` 表头和 Frequency 正确
- ownership 与 `original_query_id` 映射一致

成功时最后会出现：

```text
sample sanity OK: fold 1, 1600 queries, 32 workloads
sample sanity OK: fold 2, 1600 queries, 32 workloads
```

单独重新检查 sample：

```bash
./run_fold_error_profiles.sh check-samples
```

## 2. 先 dry-run error profile

不连接数据库，只确认 builder 能发现所有 sample：

```bash
./run_fold_error_profiles.sh dry-run-profiles
```

## 3. 建议先生成一个 template

```bash
FOLDS=1 WORKLOAD=17-0_cardinality \
./run_fold_error_profiles.sh profiles
```

如果需要指定 PostgreSQL：

```bash
FOLDS=1 \
WORKLOAD=17-0_cardinality \
PGHOST=/tmp \
PGPORT=5432 \
PGDATABASE=imdbloadbase \
PGUSER=postgres \
./run_fold_error_profiles.sh profiles
```

这个命令会：

1. 先检查 Fold 1 的 samples
2. 生成 `17-0_cardinality/error_profile/*.txt`
3. 自动运行 profile sanity check

## 4. 全量生成两个 folds

单个 template 测试成功后：

```bash
./run_fold_error_profiles.sh profiles
```

或者明确指定数据库：

```bash
PGHOST=/tmp \
PGPORT=5432 \
PGDATABASE=imdbloadbase \
PGUSER=postgres \
./run_fold_error_profiles.sh profiles
```

这一步可能耗时较长，需要 instrumented PostgreSQL 正常运行。

## 5. 单独运行最终 sanity check

```bash
./run_fold_error_profiles.sh sanity
```

Profile sanity check 会验证：

- 64 个 workload 都有 `error_profile/`
- 每个目录至少有一个 `*.txt`
- profile 文件非空
- 每行恰好两列数字
- true/estimated selectivity 都是有限正数

成功时会看到：

```text
profile sanity OK: fold 1, 32 workloads, ...
profile sanity OK: fold 2, 32 workloads, ...
```
