## 2026-08-26 Separate Error Profile Workflow

统一入口是 [run_fold_error_profiles.sh](/Users/novacx0222/RobDP/PG-PAR2QO/run_fold_error_profiles.sh)，Python 适配和检查逻辑在 [fold_error_profile_inputs.py](/Users/novacx0222/RobDP/PG-PAR2QO/code/fold_error_profile_inputs.py)。

### 1. 三类目录及用途

| 目录 | 用途 | 是否按 fold 分开 |
|---|---|---|
| `/data/robdp/imdb-error-profile-0826` | original RobDP 使用的共享 error profile；同时提供生成 fold sample 所需的原始 SQL 和 predicate | 否，所有 fold 共享 |
| `/data/robdp/imdb-separate-ep-0826` | fold ownership 输入 | 否，只保存 fold 信息 |
| `/data/robdp/imdb-separate-ep-0826-{1,2}` | 新生成的 sample、metadata 输入和各 fold 自有的 error profile | 是 |

fold ownership 文件必须位于：

```text
/data/robdp/imdb-separate-ep-0826/
  folds/
    robdp_fold_1.csv
    robdp_fold_2.csv
```

共享 reference 目录应包含：

```text
/data/robdp/imdb-error-profile-0826/
  <q>-0_cardinality/
    raw_data/
      <q>-0_testing.json
      <q>-0_testing.txt
    error_profile/
      *.txt
```

生成完成后，每个 fold 的结构是：

```text
/data/robdp/imdb-separate-ep-0826-1/
/data/robdp/imdb-separate-ep-0826-2/
  ownership.csv
  manifest.csv
  <q>-0_cardinality/
    raw_data/
    sample-50.csv
    error_profile/
      *.txt
```

重要：`/data/robdp/imdb-error-profile-0826` 是 original RobDP 的共享 profile，不会被 separate-profile 流程覆盖。`OUTPUT_PREFIX=/data/robdp/imdb-separate-ep-0826` 只会写入带 `-1`、`-2` 后缀的目录。

### 2. 统一运行环境

```bash
cd /data/robdp/PAR2QO

export SOURCE_ROOT=/data/robdp/imdb-separate-ep-0826
export REFERENCE_ROOT=/data/robdp/imdb-error-profile-0826
export OUTPUT_PREFIX=/data/robdp/imdb-separate-ep-0826

export PGHOST=/tmp
export PGPORT=5432
export PGDATABASE=imdbloadbase
export PGUSER=hx68
```

脚本默认值已经是以上三个 `/data/robdp` 路径；显式 `export` 是为了让运行日志和后续 shell 命令更清楚。

开始前可以检查输入：

```bash
test -f "${SOURCE_ROOT}/folds/robdp_fold_1.csv"
test -f "${SOURCE_ROOT}/folds/robdp_fold_2.csv"
test -d "${REFERENCE_ROOT}"
```

### 3. 生成两个 fold 的 sample，并做 sanity check

```bash
./run_fold_error_profiles.sh samples
```

它会生成：

```text
/data/robdp/imdb-separate-ep-0826-1
/data/robdp/imdb-separate-ep-0826-2
```

并检查：

- 两个 fold ownership 不重叠；
- 每个 fold 的 query 和 template 数量正确；
- SQL JSON、predicate TXT 和 `sample-50.csv` 数量正确；
- ownership 与 `original_query_id` 映射一致。

单独重新检查：

```bash
./run_fold_error_profiles.sh check-samples
```

### 4. 生成或刷新共享 querylet metadata

metadata 只需要从 fold 1 构建一次，之后 fold 1、fold 2 共同使用：

```bash
./run_fold_error_profiles.sh metadata
```

如果 metadata 已存在，但代码、过滤规则或 PostgreSQL 记录发生变化，必须强制刷新：

```bash
REFRESH_METADATA=1 ./run_fold_error_profiles.sh metadata
```

只刷新部分 templates：

```bash
for q in 7 14 19 21 30; do
  REFRESH_METADATA=1 \
  FOLDS="1 2" \
  WORKLOAD="${q}-0_cardinality" \
  ./run_fold_error_profiles.sh metadata || exit 1
done
```

刷新成功时，每个 template 都应该出现：

```text
build metadata: <q>-0
metadata sanity OK: <q>-0, ... querylets
```

只检查 metadata：

```bash
./run_fold_error_profiles.sh check-metadata
```

### 5. 生成新 error profiles

建议先测试一个 template 和一个 fold：

```bash
FOLDS=1 WORKLOAD=17-0_cardinality \
./run_fold_error_profiles.sh profiles
```

测试成功后生成两个 fold：

```bash
FOLDS="1 2" ./run_fold_error_profiles.sh profiles
```

输出分别写入：

```text
/data/robdp/imdb-separate-ep-0826-1/<q>-0_cardinality/error_profile
/data/robdp/imdb-separate-ep-0826-2/<q>-0_cardinality/error_profile
```

### 6. 最终 sanity check

```bash
./run_fold_error_profiles.sh sanity
```

它会验证 sample、metadata 和 profile，包括：

- 每个 fold 的 workload 覆盖；
- `error_profile/*.txt` 非空；
- 每行恰好两列数字；
- true/estimated selectivity 都是有限正数。

### 7. RobDP 运行时应选择的 profile

original RobDP 对所有 fold 继续使用共享目录：

```text
/data/robdp/imdb-error-profile-0826/<q>-0_cardinality/error_profile
```

separate error profile 实验分别使用：

```text
fold 1 -> /data/robdp/imdb-separate-ep-0826-1/<q>-0_cardinality/error_profile
fold 2 -> /data/robdp/imdb-separate-ep-0826-2/<q>-0_cardinality/error_profile
```

不要让 original RobDP 指向 `imdb-separate-ep-0826-{1,2}`，也不要让 separate 实验继续指向共享的 `imdb-error-profile-0826`。
